# 发布签名配置模板
#
# 使用方法：
# 1. 用 keytool 生成密钥库（或复用已有）：
#      keytool -genkeypair -v -keystore release.jks -alias localmusic \
#        -keyalg RSA -keysize 2048 -validity 10000
# 2. 将本文件复制为项目根目录下的 keystore.properties（该文件已被 .gitignore 忽略）
# 3. 填入下方真实值后，执行 assembleRelease 即可自动签名；
#    若该文件不存在，release 构建仍可运行，只是产物为未签名 APK。
storeFile=release.jks
storePassword=CHANGE_ME
keyAlias=localmusic
keyPassword=CHANGE_ME
